WELCOME_EMAIL_ENV_LABEL=DEV
# Dev knob (§3.1.2 checklist item 5): plus notice ~1 min after confirm instead of 24h,
# so the email_plus flow is testable same-day. Production env files must NOT set this.
CONSENT_PLUS_NOTICE_DELAY_MINUTES=1
